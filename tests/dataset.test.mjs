import {test} from 'node:test';import assert from 'node:assert/strict';
import {SCHEMA,DEFAULTS,serializeDataset,parseDataset,validateSnapshot} from '../site/core.js';
const record=()=>({schema:SCHEMA,id:'test-only',createdAt:'2026-09-13T00:00:00Z',teacher:{name:'test',sha256:'a'.repeat(64)},settings:{...DEFAULTS},position:{size:9,board:Array(81).fill(0),setup:Array(81).fill(0),moves:[],komi:7.5,toPlay:1,initialPla:1},snapshots:[]});
test('dataset JSONL round-trip preserves teacher provenance and position',()=>{const a=record();a.notes='劫争\nmultiple lines';assert.deepEqual(parseDataset(serializeDataset([a,a])),[a,a]);assert.deepEqual(parseDataset(JSON.stringify(a)),[a]);});
test('dataset import rejects wrong dimensions, missing hashes and malformed data',()=>{const a=record();a.position.board.pop();assert.throws(()=>parseDataset(JSON.stringify(a)));const b=record();b.teacher.sha256='unknown';assert.throws(()=>parseDataset(JSON.stringify(b)));assert.throws(()=>parseDataset('{broken}'));});
test('snapshots cannot silently export missing or non-finite teacher arrays',()=>{assert.throws(()=>validateSnapshot({actualRootVisits:1},9));const f={actualRootVisits:12,rootInfo:{winrate:.5,scoreLead:0,scoreStdev:3},rootValue:{whiteWin:.5},moveInfos:[],policy:Array(82).fill(0),ownership:Array(81).fill(0),ownershipStdev:Array(81).fill(0)};assert.equal(validateSnapshot(f,9),f);f.ownership[0]=NaN;assert.throws(()=>validateSnapshot(f,9));});
